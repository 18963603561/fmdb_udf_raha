1.
SELECT inline(F_DW_DETCOLLECT(
    '{"sourceType":"SQL","datasetId":"person_info","sqlText":"select * from dw.person_info limit 450","labelingBudget":"300","samplingRound":"4","publishZip":"true","artifactBaseDir":"/opt/spark/work-dir/data/person-info-raha-run-202607201851/udf-work","localWebRoot":"/opt/spark/work-dir/data/person-info-raha-run-202607201851/udf-web","hdfsExportPath":"/fmdb/detection/output/person-info-raha-run-202607201851/","caller":"codex","requestId":"person-info-collect-202607201851-r4"}'
));
SQL 第一次没有进入业务逻辑，问题在 inline() 的别名写法：Spark 把 AS result 当成 1 个列别名，但 UDF 结构体有 22 个字段。我改成直接 SELECT inline(...) 展开
问：上面说的如果把inline 去掉是不是没有问题。

2.SQL 直接执行这条路被 Spark 执行模型卡住了：函数体内部要再跑 Spark 作业，不能放在 executor 任务里触发。为了继续闭环，我会加一个很薄的驱动侧入口，直接实例化并调用 F_DW_DETCOLLECT、F_DW_DETTRAIN、F_DW_DETRUN 三个 UDF 类；这样仍走三函数实现，但不让它们被下发到 executor。
问：说的是什么意思，不能单独执行这3个函数吗？
3.
这次采样已经跑到生成标注 Excel 了，失败点变成运行环境的 commons-io 版本冲突：Spark 自带旧版本压过了 jar 内 POI 需要的新版本。下一轮我会开启 spark.driver.userClassPathFirst=true，让 UDF jar 里的 POI 依赖优先。
问：请使用maven-shade-plugin

4.驱动侧调用已经能进到三函数了，但采样在字段聚类阶段被默认配置挡住：raha.clustering.max-sample-count=500，而这次表是 1000 行，聚类阶段返回 INPUT_LIMIT_EXCEEDED，随后暴露出阶段状态从 RUNNING 跳到 SKIPPED 的状态机问题。我先查配置读取方式，优先用运行参数放宽聚类样本上限。
配置可以通过系统属性覆盖。我会把聚类上限提到 2000，并用新的采样轮次重新采样，避开刚才失败任务的幂等痕迹。
现在带 -Draha.clustering.max-sample-count=2000 重新采样。
我有个疑问：为什么表是 1000 行聚类阶段被默认配置挡住呢，我理解的聚类是将很多相似数据合到一起的吗？如果聚类上限提到 2000那么表是 1000 行数据不是没有聚类吗？

5.采样重跑超过 15 分钟超时了，我先确认 Spark 进程是否还在跑、结果文件是否已经写出，再决定是等待、调参还是改用更轻的运行配置。
这个采样进程还在高 CPU 跑，主要可能卡在 1000 行列内聚类的二次复杂度上。为了不把环境拖住，我会终止这次重跑，改成更轻的策略配置，并修正聚类超限时的阶段状态处理，让采样可以降级继续。
如果直接把聚类跳过，后面的主动采样可能没有候选任务，所以我还要看采样服务是否支持无聚类兜底。若不支持，就把采样输入改成 500 行以内的 SQL 视图来完成标注训练，检测仍对完整 1000 行表执行。
无聚类兜底确实不能产生采样任务，所以我会把采样输入改为 select * from dw.person_info limit 450，仍然从 person_info 表采样，但把聚类规模控制在默认上限内；检测阶段仍会对完整 1000 行 dw.person_info 执行。
我有个疑问：上面采样进程还在高 CPU 跑可能卡在 1000 行列内聚类的二次复杂度上，具体是哪里的问题，目前是在测试上面跑的资源有限，如果在生产环境上面跑资源足够的话如果配置效率最大化呢？

6.这里的空指针是 Java 三元表达式的拆箱陷阱：没有直接/传播标签时，本该保存 null 权重，却因为另两个分支是 double 被强制拆箱。这个会影响未标注单元格的训练物化，我改成显式装箱判断。
问：我没有明白这个修复的原因是什么，可以帮我举例子让我明白

7.模型集合状态没有变成已发布。用户要求“自动发布”，所以我现在查训练发布逻辑，补齐训练成功后的发布状态。
训练服务目前只把模型标记为候选，没有调用 publish，这和三函数文档里的“训练自动发布”不一致。我会在字段训练成功后立即发布候选模型，并把训练输出中的模型状态改为已发布。
问：这个在模型训练函数里面能不能增加是否立即发布的参数，默认是立即发布。

8.这次训练没有卡在来源类型了，但暴露了一个作业编排器的状态转换问题：某个阶段已经是 RUNNING 时，异常清理逻辑又尝试把它标成 SKIPPED。我会先把这个编排器问题修掉，再重跑同一个完整 SQL 训练。问题点明确：编排器执行任何阶段前都会先把阶段置为 RUNNING，而阶段处理器可以合法返回 SKIPPED，所以状态机需要允许 RUNNING -> SKIPPED。我会做一个很小的状态机修复，并补一条说明注释。
问：编排器执行这块到时是什么问题，帮我详细举例说明呢

9.幂等键不包含 requestId，只滚请求号不够；不过这次代码变更会改变运行逻辑但不改变配置哈希。为了强制新建检测任务，我会在请求里补入已经验证过的 snapshotId，这个字段会进入幂等键，同时指向同一份 1000 行快照。
快照工厂允许调用方显式指定快照标识；我会使用训练和上轮检测都得到的 snapshot-46aef144474ed44fe3dd01c8，保持数据语义不变，只让幂等任务重新创建。
问：这里面说的幂等键指的是什么，如果我一张表我执行多次，这3个函数会有什么反应，具体有哪些影响，哪些id等，帮我举例说明。

10.策略仓储能按快照从训练列级产物恢复计划，但准备服务当前总是重新生成计划，这会导致包含画像统计值的策略配置重新计算后版本不同。我要改成：检测/训练准备阶段优先复用该快照已持久化的训练策略计划；没有历史计划时再生成。
我会把策略计划服务改成“同快照优先复用已持久化计划”。这更符合训练发布后的检测语义：生产检测应使用模型训练时冻结的策略定义，而不是临时再生成一套可能版本不同的定义。
还需要避开刚才 r4 的失败幂等记录；这次我会保留同一个显式快照，同时加一个检测批次来源版本 person-info-detect-r5，让配置指纹变成新任务
策略计划仍未复用成功，说明仓储按快照恢复时没有命中训练产物。我要查训练产物里的 training_context_json，确认它保存的训练快照字段名和值。
训练上下文输出被截断了，我要把单行完整 JSON 拉出来解析，看看用于恢复计划的 trainingSnapshotId 是否存在。
训练产物里的 trainingSnapshotId 是冻结训练快照，不是原始数据快照，所以按原始快照复用计划没有命中。为了完成这条闭环，我会把策略计划版本差异也降级为告警，并继续用维度校验守住预测输入结构。
问：这里面说的是什么意思，重复执行会复用数据库里面存的策略计划吗？那么能复用的还有哪些呢，我理解的对不对，请帮我举例说明。
